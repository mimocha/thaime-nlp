# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/
*.egg

# Jupyter
.ipynb_checkpoints/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Large data files (keep tracked data small)
*.pkl
*.pickle
*.h5
*.hdf5

# Raw corpus data (downloaded via scripts, not version controlled)
data/corpora/raw/

# Environment
.env
*.env
.claude/

# uv
uv.lock

# Experiment outputs that shouldn't be tracked
# (large generated files — commit summaries and key results, not raw dumps)
experiments/*/data/raw/
experiments/*/data/*.csv.gz

# Pipeline outputs (regenerate via pipeline code)
pipelines/*/outputs/
pipelines/*/output/

# Large experiment intermediates (regenerable)
experiments/006-frequency-scoring/per_corpus_frequencies.json
experiments/006-frequency-scoring/results_raw.json
